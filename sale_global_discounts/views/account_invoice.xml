<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="discount_account_invoice_view_form_inherit" model="ir.ui.view">
            <field name="name">discount.account.invoice</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='amount_untaxed']" position="replace">
                    <field name="amount_untaxed_dis" readonly="1" invisible="1"/>
                    <field name="total_discount_amount" readonly="1" invisible="1"/>
                    <field name="amount_untaxed" invisible="1"/>

                </xpath>

            </field>
        </record>

    </data>
</odoo>
